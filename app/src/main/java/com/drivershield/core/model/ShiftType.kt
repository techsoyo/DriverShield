package com.drivershield.core.model

enum class ShiftType {
    NORMAL,      // Turno estándar
    EXTENDED,    // Turno ampliado (horas extra)
    NIGHT,       // Turno nocturno
    SPLIT        // Turno partido
}
